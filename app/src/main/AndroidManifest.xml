<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.goldian.fishingsite">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Controller_View.FragmentActivity"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".Controller_View.RegisterActivity"/>
        <activity android:name=".Controller_View.DetailJoranActivity"/>
        <activity android:name=".Controller_View.DetailBenangActivity"/>
        <activity android:name=".Controller_View.DetailKailActivity"/>
        <activity android:name=".Controller_View.LoginActivity"/>
        <activity android:name=".Controller_View.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>